name: C/C++ CI

on:
  push:
    branches: [ master, dev-2.2.4 ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        submodules: recursive
    - name: make check
      run: make check
